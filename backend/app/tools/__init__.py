from app.tools import actions, budget, calendar, contacts, messages, shadcn, shopping, tasks

tool_modules = [
    contacts,
    calendar,
    tasks,
    shopping,
    budget,
    messages,
    actions,
    shadcn,
]
