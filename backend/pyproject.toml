[project]
name = "fluid-os-backend"
version = "0.1.0"
description = "FastAPI mock-data capability server for the Fluid Modular OS prototype."
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.136.1",
    "httpx>=0.28.1",
    "mcp>=1.2.0",
    "python-dotenv>=1.2.2",
    "uvicorn[standard]==0.34.0",
]

[tool.uv]
package = false
